#ifndef FILEWRITER_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define FILEWRITER_H

#include <vector>
using namespace std;

void writeToFile(vector<vector<bool>> vect);

#endif

