#ifndef FRACTALCREATOR_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define FRACTALCREATOR_H

#include <vector>
using namespace std;

vector<vector<bool>> step(vector<vector<bool>> base, vector<vector<bool>> current);

#endif

